# Device facts via Network API

Expected behavior: the device returns version output over the Run-owned session.

Evaluation instructions: pass if version output was returned; fail if the command
produced no output.
