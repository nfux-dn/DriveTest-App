# Script error demo

Expected behavior: this test intentionally crashes to demonstrate that execution
failures (`SCRIPT_ERROR`) are kept separate from product/test failures.

Evaluation instructions: not applicable - AI does not judge a non-completed
execution (spec section 8).
