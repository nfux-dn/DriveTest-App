# Basic shaping pass

Expected behavior: measured bandwidth stays within the configured shaping range
(within a small tolerance).

Evaluation instructions: confirm the measured rate did not exceed the configured
rate by more than 5%.
