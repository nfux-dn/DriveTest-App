id: lab_optics
name: Lab Optics
platform: platform_c
system_type: optics
software_version: "24.1"
enabled: true

# Intentionally NOT compatible with the Demo Shaping Suite: wrong platform,
# missing shaping/pwhe capabilities, and no traffic generator. Used to show the
# compatibility matcher filtering environments out.
capabilities:
  - optics

metadata:
  devices:
    - role: dut
      name: optics-dut-1
